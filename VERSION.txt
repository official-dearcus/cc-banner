cc-banner  build 2026-08-18-A
처마 컬러 섹션 — cheoma_color.fig 실측 반영 + 흰 박스 (2026-08-18)
  공통(shared) 모드 치수를 .fig 값으로 확정했다. 누볼라(lines)는 예전 그대로.

                지금                    .fig 실측
    흰 박스     없음(01·02)             x50 w760 · 위아래 여백 52 · #ffffff
    칩 폭       비율에서 계산            349 고정
    칩 좌우     12                      12  (같음)
    행 간격     38                      20
    이름 글자   24px                    28px
    사진~이름   4                       0
    배치        2열 1-2-2               2열 1-2-2  (같음)

  흰 박스는 템플릿 01·02·03 모두 깐다 — 제품 사진이 배경에 묻히던 문제.
  누볼라 03 의 기존 흰 박스(폭 800)는 그대로 뒀다.
  칩 높이는 349 / 사진비율 로 나온다(9:5 사진 → 194).
  colorRowGap 을 시트에 적으면 여전히 덮어쓸 수 있다(기본 20).

  ⚠ .fig 은 첫 칩만 사진 높이가 156, 나머지는 100 이다(사진마다 여백이 달라서로 보인다).
    코드는 한 제품군에 한 비율을 쓰므로 전부 같은 높이로 그린다.
    사진마다 높이를 다르게 하려면 별도 작업이 필요하다.

이전:
칩 칸 비율을 실제 사진에서 자동으로 (2026-08-15 신고)
  "rowGap 을 20 으로 줄였는데 여전히 벌어진다" 의 원인은 rowGap 이 아니었다.
  colorRatio 를 안 넣으면 칸이 세로형 규격(120×220)으로 잡히고,
  가로로 긴 사진이 그 안에 들어가며 위아래로 77px 씩 빈칸이 생겼다.
    사진 아래끝 ~ 다음 사진 위끝 = 77 + 4 + 40 + 20 + 77 = 217px
  nvColorRatio 순서를 바꿨다:
    ① 시트 colorRatio  ② 실제 불러온 색상 사진의 비율  ③ 기본 240/440
  ②가 생겨서 시트에 아무 것도 안 적어도 빈칸이 안 생긴다.
    9:5 사진 → 칸 317×176 · 빈칸 0 · 사진 사이 64px (4 + 40 + rowGap 20)
  세로 사진(240×440)인 누볼라는 값이 그대로다.

  참고 — 사진 사이 간격은 rowGap 만이 아니다
    사진 아래 ~ 이름칸 4 (chipLabelGap)
    이름칸 40 (chipLabelH · 글자는 가운데)
    행 간격 rowGap
  이름칸을 더 줄이려면 chipLabelH 를 손봐야 한다(지금은 .fig 값 그대로).
