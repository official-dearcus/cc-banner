cc-banner  build 2026-07-29-v
u 빌드에서 카드가 빈 채로 남던 문제 수정
  원인: renderTplList() 가 캔버스를 새로 만드는데 서명 캐시가 "변화 없음"으로 판단해
        그리기를 건너뛰었다 → 새 캔버스가 빈 채로 남음
  수정: ① renderTplList 직후 서명 무효화
        ② 이미지 로드 완료 시 한 번 다시 그림
        ③ 그 템플릿 히어로가 없으면 현재 히어로로라도 그림 (빈 카드 방지)
유지: 카드마다 자기 템플릿 히어로 · 셀러명/타이틀/히어로 변경으로는 다시 그리지 않음
