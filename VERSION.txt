cc-banner  build 2026-09-01-G
인스타 피드 이벤트를 .fig "feed event" 실측으로 다시 그림 (2026-09-01)
  ⚠ 그동안 상세용 event_comp 를 배율(1.26~1.30)로 늘려 피드를 만들고 있었다.
    .fig 에는 피드 전용 "feed event" 컴포지션이 따로 있었고 설계가 아예 다르다.
    앞선 "3개 넣으려고 카드를 0.85 로 축소" 같은 임시방편은 전부 버렸다.

  .fig 실측 (1080×1350)
    txt-wrap   (40,115) 1000×140   sub 60/lh48 · gap20 · title 92/lh72
    흰 박스     (56,315) 968×974
    event_list  860 폭 · 박스 안 가로 가운데(54/54) · 세로 가운데
    카드        [글 520] 12 [이미지 328×228]   높이 = max(글, 228)
      이름 40 Bold  lh32 #605a4b ls-2%
      본문 36 Reg   lh52 #555555 ls-2%
      주석 24 Light lh38 #adadad ls-6%   (ev.note 있을 때만)
    카드 사이   구분선 · 위아래 32 (총 64)
    검산 3장: 228 + 250 + 228 + 64×2 = 834 = .fig list 높이 ✓

  달라진 것
    · 흰 박스가 생겼다 (예전엔 아예 없었다)
    · 이미지 240 정사각 → 328×228 (1.44:1) · .fig 과 같은 crop
    · 글 폭 424×배율 → 520 고정 · 글자 40/36/24 로 확정 (배율 안 먹임)
    · 주석 줄("※ ID당 1ea 증정") 지원 — ev.note
    · 3장이 기준 크기로 그대로 들어간다. 4장이면 3+1 로 나뉜다.
      더 넣어야 하면 이미지 높이를 150 까지 줄여 맞춘다(.fig 4장 변형과 같은 방식).
  템플릿 01·02·03 모두 같은 배치. 헤더 글꼴·색만 템플릿을 따른다.

이전:
1) 뱀부에서 써볼래요 이벤트가 안 뜨던 문제 (2026-09-01 신고)
   legacySecOrder 의 키 목록에 "try" 를 빠뜨렸다 → 피드 계획에서 통째로 잘렸다.
   상세에는 안 그려지지만(LEGACY_SECTIONS 에 없다) 피드 순서에는 있어야 한다.
   hero → opt  ⇒  hero → opt → try  (feedCount 2 → 3)

2) 한 장에 이벤트 3개까지 · 아래 여백 축소 (요청 2026-09-01)
   EVF.bottom 40 → 20 · maxPerSlide 3 신설.
   ⚠ 아래 여백만으로는 부족했다 — .fig 카드 크기 기준 3장 필요/가용:
       01  1141 / 975   02  1003 / 984   03  1031 / 963
     여백을 0 으로 해도 01·03 은 안 들어간다.
   → 3개가 빠듯하면 "카드만" 조금 줄인다(evfShrinkFor). 제목·상단 여백은
     그대로라 앞서 맞춘 상단 정렬이 유지된다. 최소한만 줄인다(1.00→0.84).
       실제 배율  01 0.85 · 02 0.99 · 03 0.87
       맨 아래 y  1326 / 1327 / 1319  (캔버스 1350)
     2개 이하일 땐 배율 1 — 지금까지와 크기가 같다.
     4개면 3+1 로 나뉜다. 써볼래요도 같은 규칙을 쓴다.
   ⚠ 01 이 제일 많이 줄어든다(15%). 본문 폭이 740 이라 피드 확대율이
     1.297 로 제일 크기 때문이다. 눈으로 보고 과하면 알려줄 것.
