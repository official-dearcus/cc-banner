cc-banner  build 2026-08-13-G
향 섹션 삼각형 — 피그마 정다각형 방식으로 (2026-08-13 재신고)
  F 에서 꼭짓점을 아래로 뒤집은 건 오판이었다. 되돌린다.
  패스 블롭을 직접 뜯어보니 답이 나왔다:
    상자 10×296 인데 실제 좌표는 (5,0) → (9.33,222) → (0.67,222)
  피그마 REGULAR_POLYGON 은 상자에 내접하는 "타원 위"에 꼭짓점을 찍는다
  (각도 -90 / 30 / 150). 그래서 삼각형이 상자를 꽉 채우지 않는다:
    꼭짓점은 위(맞음) · 밑변은 바닥이 아니라 높이의 75% 지점
  상자 전체로 그렸던 게 진짜 원인이었다 —
    잘못  밑변 y 904 → 00A(849~901) 를 덮음
    정정  밑변 y 830 → 00A 위에서 끝남 (피드도 1043 < 1069)
  검산: 계산값 (9.33, 222.0) 이 블롭 실측과 소수점까지 일치.

이전:
향 섹션 삼각형 방향 뒤집음 — 꼭짓점이 아래다 (2026-08-13 신고)
  위로 그렸더니 아래쪽이 두꺼워져 "00A" 글자를 덮었다. 상세·피드 둘 다.
  .fig 좌표로 검산하면 삼각형과 00A 는 세로로 겹치게 배치돼 있다.
    상세  삼각형 y 608~904 · 00A y 849~901
    피드  삼각형 y 766~1136 · 00A y 1069~1128
  겹치는 구간의 삼각형 폭
    꼭짓점 위(전)   상세 8.1~9.9px · 피드 13.1~15.7px   → 글자를 덮는다
    꼭짓점 아래(후) 상세 1.9~0.1px · 피드 2.9~0.3px     → 실오라기라 안 덮는다
  즉 .fig 은 꼭짓점이 아래인 걸 전제로 배치돼 있다.
