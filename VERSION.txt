cc-banner  build 2026-09-04-I
하단 카피 띄어쓰기 — 썸네일까지 다섯 곳 (2026-09-04 추가 확인)
  H 에서 네 곳으로 적었는데 실제로는 다섯 곳이다.
    nvMain02   신형 상세   (원래부터 있었다)
    nvfHero02  신형 피드   ← 없었다
    drawThumb  썸네일      ← 없었다
    hero02     구형 상세   ← 없었다
    feedHero   구형 피드   ← 없었다
  다섯 곳 모두 copyGap(ctx) 를 쓴다.
  간격은 공백 한 칸 = 글자크기 × 0.33 (32px → 11 · 44px → 15).
  ⚠ "신형도 붙어 나온다"는 신고는 신형 피드·썸네일 이야기였다.
    신형 상세는 원래 띄어져 있었다.

이전:
하단 카피 — 일반 문구와 볼드 문구 사이 띄어쓰기 (요청 2026-09-04)
  신형 상세에만 있었고 나머지 세 곳은 붙어 나왔다
  ("…안심하고 쓰는뱀부 500 시리즈!").
  copyGap(ctx) 를 05-state-utils 로 빼서 네 곳이 같이 쓴다.
    12-render-nuvola  상세(기존 인라인 → 공통) · 피드
    14-render-hero    구형 상세
    15-render-thumb-feed  구형 피드 2곳
  시트 값 끝이나 볼드 앞에 이미 공백이 있으면 넣지 않는다(중복 방지).
  ⚠ 호출 전에 ctx.font 가 "일반" 상태여야 공백 폭이 맞는다.
  구형/신형 · 상세/피드 네 곳 모두 간격 확인.
