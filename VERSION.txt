cc-banner  build 2026-09-03-H
화면 재구성 — 좌측 세로 탭 (요청 2026-09-03 · 시안 4장)
  패널 항목이 14개까지 늘어 한 화면에 안 들어왔다. 네 갈래로 나눈다.
    제품군  제품군 목록 · 테마 통일 · 행사 정보 수정
    데이터  데이터 소스 · 제품군 · 셀러명 · 히어로 타이틀 · 공구 기간 ·
            하단 카피 · 시리즈 제목 · 가격 카드 · 이벤트 · 써볼래요 이벤트
    템플릿  템플릿 · 컬러 · 히어로 이미지 (+ 누볼라 추가 블록)
    순서    섹션 순서

  scripts/30-nav.js 신설. 블록마다 index.html 에 data-nav 를 적어 뒀다.
  ⚠ 블록을 옮기지 않고 감추기만 한다(.navoff). 기존 코드가 id 로 찾아 쓰기 때문에
    DOM 에서 빼면 여기저기 깨진다.
  번호는 renumberBlocks 가 보이는 것만 세므로 탭마다 1부터 다시 매겨진다.
    ⚠ 예전엔 offsetParent 로만 판단해 레이아웃 계산 전에는 전부 1 이 됐다
      → navoff/hidden 을 직접 보게 고쳤다.
  선택한 탭은 localStorage(ccb.nav.v1) 에 남는다. 제품군을 고르면 데이터 탭으로 이동.

  배치
    로고    CC 배너 제너레이터 → CC BANNER
    줌      미리보기 슬라이더는 캔버스 위 오른쪽
    내보내기 1x/2x · 공유코드 · 받을 항목 · 다운로드를 화면 오른쪽 아래로 모음
            (.exportdock · position fixed) — 받을 항목 팝오버는 위로 열린다
    그리드  rail 88px + left + resizer + center + right

  기존 기능은 그대로다(주요 요소 13개 존재 확인 · 회귀 13종 이상 없음).

이전:
이미지 불러올 때 버벅이던 문제 최적화 (2026-09-03 신고)

1) 다시 그리기를 한 프레임으로 모은다 (scripts/29-perf.js 신설)
   draw() 한 번이 상세 캔버스 전체 + 열려 있는 피드 슬라이드 전부 +
   패널 세 곳(섹션 순서·받을 항목·써볼래요)을 다시 그린다.
   그런데 이미지가 하나 로드될 때마다 draw() 를 불렀다 —
   선물 10개면 로딩 중에 열 번 넘게 전체를 다시 그린 셈.
   draw() 를 예약제로 바꿨다. 같은 프레임에 여러 번 불려도 실제로는 한 번.
     검증: draw() 10번 → 실제 그리기 1번
   ⚠ 29-perf 는 맨 마지막에 로드돼야 한다(26·27·28 이 draw 를 감싸므로).
   즉시 그려야 하는 곳은 drawNow(). 다운로드 버튼은 누를 때 한 번 비운다.

2) 같은 계산을 반복하던 것 정리
   exportSections 가 섹션 키마다 feedPlanBySection() 을 다시 만들었다
   (6개면 전체 피드 계획을 6번). 한 번 만들어 찾아 쓴다.
   dlCount 도 dlSections() 를 또 부르던 걸 넘겨받게 했다.

3) 카드 치수 메모이제이션 (EVF_M_CACHE)
   evFeedFit 이 묶음을 늘려 가며 재고, evfImgH 가 이미지 높이를 낮춰 가며
   다시 부르기 때문에 같은 카드를 수십 번 재고 있었다.
   키에 실제로 그려질 문구를 넣어 시트를 다시 불러와도 낡지 않는다.

   글자 폭 계산 횟수 (선물 6개 기준)
     받을 항목 그리기   3474 → 177
     피드 계획 만들기    260 → 0
     evFeedGroups        260 → 26
