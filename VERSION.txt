cc-banner  build 2026-08-12-G
"구매 선착순10명에게" → "구매 선착순 10명에게" 띄어쓰기 (요청 2026-08-12)
  ⚠ .fig 은 "구매 선착순"·"10"·"명에게" 를 간격 0 으로 붙여 놨다(144→144→177).
    붙여 읽히므로 요청대로 띄운다 — 이 부분만 .fig 과 의도적으로 다르다.
  evTokens 와 evSentence(패널 미리보기) 둘 다 반영.
  낱말 단위 줄바꿈이라 "선착순 / 10명에게" 로 접힐 수는 있다.
  .fig 카드 높이 대조: 280/320/280 · 240/280/240 · 260/300/260 그대로.

이전:
1) 패널 섹션 순서 — 히어로 이미지 ↔ 이벤트 (요청 2026-08-12)
   마크업 순서만 바꿨다. 번호는 renumberBlocks 가 다시 매긴다
   (누볼라는 09-hero-color 가 블록을 끼워 넣어 실제 번호가 14·15 로 밀린다).

2) 이벤트도 드래그로 순서 변경
   가격 카드(11-rows-picker)와 같은 방식 — ⠿ 손잡이 · over-t/over-b 표시.
   설정 [5]와 패널 이벤트 둘 다 적용(같은 편집기라 한 번만 붙이면 된다).

3) 선물 수량 (qty)
   문장   1개  "…수세미를 드립니다."
          2개  "…수세미 2개를 드립니다."
   ⚠ 조사는 수량이 붙으면 "…2개" 기준으로 다시 고른다.
     GiftMaster.particle 은 수량 1일 때만 존중한다 — "수세미를"로 적어둔 값이
     "수세미 2개를"에서도 맞다는 보장이 없기 때문.
     (예: 네이버 포인트 3천원"을" → 3천원 2개"를")
   evSentence() 신설 — 패널 미리보기와 배너가 같은 함수를 쓴다.

   이미지 여러 장 (evDrawGift / EV_MULTI)
   ⚠⚠ .fig 에는 1개짜리 240×240 만 있다. 여러 장 배치는 디자인 근거가 없다.
     카드 높이를 절대 안 건드리는 선에서 정했다 — 같은 칸 안에서 부채꼴로 겹친다.
       s = 240 / (1 + (n-1)*0.26),  각 장을 0.26*s 씩 우하향 이동, 뒤에서 앞으로
       n=2 각 190px · n=3 158px · n=5 118px — 어느 경우도 칸 끝이 정확히 240
     5장까지만 그린다(그 이상은 겹쳐서 안 보인다). 수량 자체는 20까지 받는다.
     배치가 마음에 안 들면 EV_MULTI 의 step·max 두 값만 고치면 된다.
