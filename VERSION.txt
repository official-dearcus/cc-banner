cc-banner  build 2026-07-29-o
① 히어로 — 내장 깅엄 패턴 제거. 시트 히어로 이미지를 화면 전체에 그대로 채운다
   (assets/nuvola03-pattern.png 삭제)
② 옵션 카드 — .fig 구조대로 재배치
     option-badge_on 342×360 안에서
       name-wrap  +0   342×220  (배지 160×52 @+0 · 간격24 · 텍스트 @+76)
       price-wrap +260 342×100  (아래 고정 = flex justify-between)
     box-img +(380,20) 320×320  (카드 360 - 상하 20)
   → 배지를 +76 에 그리던 것을 +0 으로 고침
④ 테마 → 히어로 자동 매핑
   HeroMaster 에 theme 열 추가(선택). 테마 키를 적으면 그 테마 선택 시 히어로 자동 교체.
   비워두면 지금처럼 수동. 직접 업로드한 이미지는 건드리지 않음.
③ 색상 — .fig 와 대조했으나 차이를 특정 못함. 측정값은 대화 참조
