cc-banner  build 2026-09-04-E
좌측 탭 활성 상자가 첫 번째만 다르던 문제 (2026-09-04 신고)
  구분선을 버튼에 border-top + padding-top 으로 걸었다 →
  두 번째부터 상자가 50 이 아니라 61 이 되고 위 모서리가 각졌다.
  선을 버튼 바깥(여백 한가운데)에 ::before 로 띄운다. 상자는 안 건드린다.
    rail gap 21 (10 + 선 1 + 10) · 버튼 50×50 r5 네 개 모두 동일
  검증: 네 탭 어느 걸 골라도 활성 상자 50×50 r5.

이전:
누볼라 02 에서 하단 카피 입력이 막혀 있던 문제 (2026-09-04 신고)
  조건이 `nvIsOn() || state.tpl !== "02"` 였다 — 신형이면 무조건 숨김.
  그런데 nvMain02(상세)와 nvfHero02(피드)가 state.copy·copyBold 를 실제로 그린다.
  즉 배너에는 나오는데 넣을 칸이 없었다. 패밀리 조건을 뺀다.
    조건 → state.tpl !== "02"
    뱀부 02 · 누볼라 02 보임 · 01·03 숨김
  ⚠ C 에서 "신형은 하단 카피가 없다"고 적었는데 틀렸다. 기존 조건을 확인 없이
    그대로 옮긴 탓이다.
