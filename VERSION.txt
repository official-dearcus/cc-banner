cc-banner  build 2026-09-02-B
써볼래요 이벤트 배경을 이벤트 피드와 구분 (요청 2026-09-02)
  같은 계열에서 10% 만 민다 (evShade / EV_TRY_SHADE).
    밝은 배경은 검정 쪽, 어두운 배경은 흰색 쪽 —
    템플릿마다 배경 키가 달라도(01 colorBg · 02 colorBgLight · 03 accent)
    규칙 하나로 처리된다. 모든 템플릿·6색 공통.
  green 기준  01 #b9ca7d → #a7b671   02 #f0f3dd → #d8dbc7   03 #65812d → #748e42
  색차 28~40 — 같은 계열로 보이면서 구분은 된다.
  이벤트 피드 자체는 안 바뀐다(배경은 opt.bg 로만 덮어쓴다).

이전:
빈 카드를 추가해도 제품명을 못 바꾸던 문제 (2026-09-02 신고)
  제품명이 <div> 글자로만 찍혀 있었다. [+ 제품 추가] 로 만든 카드는
  "신규 제품" 그대로 굳어서 고칠 방법이 없었다.
  [옵션 수정] 을 누르면 이름도 입력칸으로 바뀐다(옵션 줄과 같은 자리에서 편집).
  ⚠ rowName() 은 name → [n1, n2] 순으로 본다(16-render-cards).
    고칠 때 n1/n2 를 지워서 옛 필드가 다시 이기지 않게 한다.
    (처음엔 name1/name2 를 지우도록 잘못 짰다가 테스트에서 잡혔다)
  타이핑 중에는 패널을 다시 안 그린다 — 커서가 튀지 않는다.
