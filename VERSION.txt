cc-banner  build 2026-08-13-B
향 섹션에 Dear.customer 도장 추가 (2026-08-13 제공 SVG)
  .fig 은 벡터 조각 102개 + 곡선 글씨(TEXT_PATH)라 코드로 못 만든다 → SVG 로 받았다.
  곡선 글씨까지 SVG 안에 들어 있어 캔버스로는 아무 글자도 안 그린다.
  assets/dearcus-stamp.svg (124×90, 단색 #b3c7da)

  SVG 장식 에셋을 공용 로직으로 묶었다 (scentAsset)
    무늬·도장 둘 다 단색이라 fill 만 통째로 갈아끼워 data URL → Image.
    파일은 한 번만 읽고 색마다 한 번만 그림을 만들어 캐시한다.
    ⚠ 무늬는 파일 viewBox(1080×1326)가 도형 범위와 달라 그대로 쓰면 어긋난다
      → 패스 bbox(-39.5 -1.5 1157 2221)로 덮어쓴다. 도장은 파일 viewBox 그대로.

  도장 배치 (.fig)
    상세 123.88×89.56 @662.17,1057.26 (deco 기준) → 화면 682,1148
    피드 157.52×113.97 @839.95,1151.03          → 화면 860,1215
  도장 색  blue #b3c7da · green #b9ca7d (.fig 실측) · 나머지는 colorBg 파생

이전:
향(scent) 섹션 신설 — .fig airbium_scent / airbium_scent_feed 실측
  scripts/25-render-scent.js  상세는 옵션 바로 아래, 피드는 옵션 다음 장.
  시트에 scentUrl 이 없으면 섹션 자체가 안 붙는다(높이 0) → 기존 제품군 무영향.

  치수 (.fig 그대로)
    상세 860×1298  무늬 895×1719 @-18,35   deco @20,91   사진 740×920 @60,201
    피드 1080×1350 무늬 1155×2218 @-38,24  deco @20,64   사진 940×1000 @70,191
  deco 글자 7종(브랜드/부제/좌우 세로글씨/코드/푸터/태그) + 삼각형.
  좌우 세로글씨는 시계방향 90° — 글자가 아래로 흐르고 상자는 왼쪽으로 뻗는다.
  사진은 cover — 원본 비율이 다르면 넘치는 만큼 잘리고 가운데로 맞춘다.

  물결 무늬 (assets/airbium-scent-pattern.svg, 2026-08-13 제공)
    .fig Vector 는 압축된 벡터 패스라 코드로 못 꺼낸다 → SVG 로 받았다.
    패스 bbox 1156.8×2220.7 이 .fig 피드 Vector(1155×2218)와 일치하고
    상세 Vector(895×1719)와도 비율이 같다 — 같은 도형의 확대/축소.
    viewBox 를 bbox 로 잡아 원하는 사각형에 그대로 끼운다.
    색은 로고와 같은 방식으로 fill 만 갈아끼워 data URL → Image. 색마다 캐시.

  색 (scentTheme)
    ⚠ .fig 에는 blue·green 두 벌뿐이고 규칙이 서로 다르다
        blue  글자 #7d9eca = colorBg   green 글자 #65812d = accent
      두 벌은 .fig 값을 그대로 박아 뒀다(SCENT_FIG).
      나머지 4색은 파생값 — 흰 배경 대비 2.2 미만이면 accent, 이상이면 colorBg.
      배경/무늬는 colorBg 를 흰색에 3% / 12% 로 섞는다.
      pink #7c6055 · yellow #b08a1e · orange #c26a2b · mint #2f8a76
      → .fig 근거가 없으므로 확인 후 조정 필요.

  ⚠ 스탬프(Dear.customer 도장)는 아직 없다. .fig 에서 벡터 조각 102개라 못 꺼낸다.
    scentStamp() 훅만 열어 뒀다 — SVG 를 받으면 그 함수만 채우면 된다.

  시트  ProductMaster.scentUrl (제품군 단위) · TemplateMaster.scentUrl (템플릿별)
        07-sheet-sync 가 scentByTpl 로 넘긴다. 템플릿별 값이 제품군 값을 이긴다.
  글자는 .fig 기본값을 쓰고 g.scent 로 덮어쓸 수 있게 열어 뒀다.
