cc-banner  build 2026-08-28-B
상세페이지 섹션 순서를 드래그로 (요청 2026-08-28)
  scripts/26-section-order.js 신설 + nvDetail 구조 변경.
    섹션 하나 = 높이 함수 + 그리기 함수 (NV_SECTIONS)
      main 히어로 · option 옵션 · scent 향 안내 · color 컬러 안내 · event 이벤트
    nvDetail 이 목록을 순서대로 돌며 ctx.translate 로 옮겨 그린다.
    ⚠ nvMain 은 y 인자가 없어 항상 0 에 그렸다 → translate 로 통일했다.
      덕분에 히어로도 순서를 바꿀 수 있다.
    nvCanvasH 도 같은 목록에서 합산 — 순서를 바꿔도 캔버스 길이는 그대로다.

  패널 [12] 섹션 순서 — ⠿ 손잡이로 드래그 (가격 카드·이벤트와 같은 방식).
    지금 안 쓰이는 섹션(높이 0)은 흐리게 "안 쓰임" 으로 표시한다.
    [기본 순서로] 버튼. 기본과 같으면 비활성.
    저장값이 깨져도 버틴다 — 모르는 키는 버리고 빠진 키는 뒤에 붙인다.
      (섹션이 새로 생겨도 예전 설정이 안 깨진다)

  ⚠ 뱀부 01·02 는 구형 렌더러(13-render-core)라 순서 고정이다.
    그 경우 패널의 순서 섹션 자체를 숨긴다.
  ⚠ 인스타 피드 슬라이드 순서는 안 건드렸다(nvFeedPlan 그대로).

이전:
뱀부 02 인스타 피드 옵션면 — 흰 박스 가운데 정렬 (2026-08-28 신고)
  x 를 60 으로 박아 놨는데 폭이 940 이라 좌 60 / 우 80 으로 10px 치우쳤다.
  폭에서 가운데를 계산한다 → x 70. 카드 1장(마지막 홀수면)도 같이 고쳤다.
  카드 사이 구분선도 940 하드코딩을 listW 로 바꿨다.
  01 은 x60 w960 이라 원래 가운데였고 흰 박스도 없어 변화 없다.
