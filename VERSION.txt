cc-banner  build 2026-09-03-D
선물 사진이 잘리던 문제 (2026-09-03 신고)
  cover 로 칸을 채우고 있었다. .fig 은 미리 1.44:1 로 잘린 사진이라 문제가
  없었지만, 실제 GiftMaster·제품 사진은 정사각·세로형이라 위아래가 잘렸다.
  원본 비율 그대로 칸 안에 넣는다(drawRatioFit) — 남는 쪽에 여백이 생길 뿐이다.
    피드 칸 328×228   정사각 960 → 228×228 · 세로 800×1200 → 152×228
                      가로 1600×600 → 328×123 · 1.44:1 → 328×228 (꽉 참)
    상세 칸 240 정사각 도 같이 고쳤다 — 가로로 긴 사진이 좌우로 잘렸다.
  이벤트·써볼래요 둘 다. 칸 안에서 가로·세로 가운데 정렬.

이전:
써볼래요 경품 드롭다운에서 GiftMaster 선물 목록 제거 (요청 2026-09-03)
  써볼래요 경품은 "그 제품군의 제품" 이라 이벤트 사은품과 섞이면 안 된다.
  드롭다운은 이제 셋뿐이다:
    제품군 제품     ProductMaster (enabled TRUE)
    옵션 외 단품    ProductMaster (enabled FALSE)
    직접 입력…      이름 + 사진 주소
  ⚠ 예전에 gift 로 저장된 값은 그대로 읽힌다(tryGifts 는 남겨 뒀다) — 안 깨진다.
  단품은 ProductMaster 에 enabled=FALSE 로 넣으면 "옵션 외 단품" 에 뜬다.
