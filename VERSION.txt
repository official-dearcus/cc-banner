cc-banner  build 2026-09-04-F
피드에서 OPTION 번호가 장마다 1 로 돌아가던 문제 (2026-09-04 신고)
  nvFeedPlan 이 옵션을 2개씩 잘라 담으면서 "전체 목록에서 몇 번째"인지를
  안 실어 보냈다. 카드 그릴 때 슬라이드 안의 i(0,1)만 넘어가
  장마다 OPTION 1·2 로 다시 시작했다.
  plan 에 base 를 넣고 (base + i) 로 넘긴다.
    옵션 5개 → 1·2 / 3·4 / 5   (base 0·2·4)
  optDark 아닌 템플릿의 카드 루프도 for…of 에서 forEach 로 바꿔 번호를 넘긴다.
  상세는 원래부터 이어졌고 그대로다(1~5 확인).

이전:
좌측 탭 활성 상자가 첫 번째만 다르던 문제 (2026-09-04 신고)
  구분선을 버튼에 border-top + padding-top 으로 걸었다 →
  두 번째부터 상자가 50 이 아니라 61 이 되고 위 모서리가 각졌다.
  선을 버튼 바깥(여백 한가운데)에 ::before 로 띄운다. 상자는 안 건드린다.
    rail gap 21 (10 + 선 1 + 10) · 버튼 50×50 r5 네 개 모두 동일
  검증: 네 탭 어느 걸 골라도 활성 상자 50×50 r5.
