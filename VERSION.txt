cc-banner  build 2026-09-02-E
써볼래요 문구 변경 (요청 2026-09-02)
  before  써볼래요 10명에게 [제품명]을 드립니다.
  after   이벤트에 선정되신 분께 [제품명] [개수][을/를] 증정드립니다.
    인원(num)은 문장에서 빠지고 제목의 "(N명)" 에만 남는다.
    개수는 2 이상일 때만 쓴다 — 1개면 "수세미를", 2개면 "수세미 2개를".
    조사는 개수까지 붙인 문자열 기준으로 다시 고른다.
  패널에 수량 칸 추가 (인원 옆, 최대 20). 새 행은 1로 시작.

  evTokens 가 ev._p1 / ev._tail 을 보면 그 문구를 쓴다 —
  이벤트는 그대로 두고 써볼래요만 다른 문장을 쓸 수 있게 한 것.
  줄바꿈·굵기(선물만 SemiBold) 규칙은 이벤트와 같다.

이전:
뱀부(구형)에서 써볼래요 색이 안 바뀌던 문제 (2026-09-02 신고)
  C 는 nvOptBg(th) 로 배경을 가져왔는데, 구형 테마에는 colorBg / colorBgLight
  키가 아예 없다(accent · circleBg · badgeBorder · sectionBg 넷뿐).
  → undefined 가 돌아와 이벤트 배경으로 되돌아갔다.
  구형 피드 옵션면(15-render-thumb-feed)이 쓰는 규칙을 그대로 쓴다:
    배경  th.sectionBg   (02 기본 #b9ca7d · 그 외 #f0f3dd)
    글자  02 는 흰색 · 그 외 th.accent
  신형은 C 그대로. 검증: 뱀부 01/02 × green·blue·pink 에서 옵션면과 일치.
