cc-banner  build 2026-09-04-D
누볼라 02 에서 하단 카피 입력이 막혀 있던 문제 (2026-09-04 신고)
  조건이 `nvIsOn() || state.tpl !== "02"` 였다 — 신형이면 무조건 숨김.
  그런데 nvMain02(상세)와 nvfHero02(피드)가 state.copy·copyBold 를 실제로 그린다.
  즉 배너에는 나오는데 넣을 칸이 없었다. 패밀리 조건을 뺀다.
    조건 → state.tpl !== "02"
    뱀부 02 · 누볼라 02 보임 · 01·03 숨김
  ⚠ C 에서 "신형은 하단 카피가 없다"고 적었는데 틀렸다. 기존 조건을 확인 없이
    그대로 옮긴 탓이다.

이전:
하단 카피를 템플릿 탭 안으로 (요청 2026-09-04)
  데이터 탭의 별도 블록("6 하단 카피 (02 전용)")이었다.
  템플릿 블록 안, 템플릿 고르는 자리 바로 아래로 옮겼다.
  번호가 붙는 블록이 아니라 딸린 입력이다(.sub · 소제목 "하단 카피").
  02 를 골랐을 때만 나온다 — 숨김 조건은 기존 그대로
  (nvIsOn() || state.tpl !== "02", 10-group-template-ui).
    뱀부 01 숨김 · 뱀부 02 보임 · 누볼라 02 숨김(신형은 하단 카피가 없다)
  데이터 탭은 9개로 줄고 번호가 1~9 로 다시 이어진다.
  템플릿 탭은 템플릿·컬러·제품 이미지 3개 그대로.
