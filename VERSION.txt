cc-banner  build 2026-08-12-I
02 썸네일 히어로를 880 폭으로 줄여 왼쪽 정렬 (요청 2026-08-12)
  1080 을 cover 로 꽉 채우면 정사각으로 잘리면서 제품이 확대돼 보였다.
  NVF.th02 에 heroW: 880, heroVA: "bottom" 추가.
    cover 가 아니라 폭만 880 으로 맞추고 비율대로 줄인다(잘라내지 않는다).
    가로는 x=0 왼쪽 고정, 남는 세로는 heroVA 로 정한다("bottom"|"center"|"top").
    정사각 원본 → 880×880, y=200 (아래 붙음)
  t02(피드)에는 안 넣었으므로 인스타 피드는 예전처럼 꽉 찬다.
  01·03 썸네일도 heroW 가 없어 기존 cover 경로 그대로다.
  세로 위치를 바꾸려면 heroVA 한 값만 고치면 된다.

이전:
선물 여러 장 배치 재작업 — 레퍼런스 역산 (요청 2026-08-12)
  before  45° 대각선 · 왼쪽이 맨 앞 (step 0.26, dx=dy)
  after   완만한 각도 · 오른쪽 아래가 맨 앞
    EV_MULTI = { step: 0.42, slope: 0.45, max: 5 }
      step   가로 간격 / 각 장 크기   3장 → 각 130 · 간격 55
      slope  세로 낙차 / 가로 간격    3장 → 낙차 25 (45°가 아니라 완만)
    가로는 항상 240 을 꽉 채우고(s 를 가로 기준으로 잡는다),
    세로는 그보다 낮으므로 칸 안에서 가운데 정렬한다(3장 → 위아래 30).
    그리는 순서를 뒤집어 배열 마지막(오른쪽 아래)이 맨 위에 온다.
  evGiftLayout(qty, D) 로 좌표 계산을 분리 — 상세·피드가 같이 쓰고 검증도 쉽다.
  카드 높이는 어떤 수량에서도 안 변한다. 5장까지만 그린다(수량 입력은 20까지).
