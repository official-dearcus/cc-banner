cc-banner  build 2026-08-18-B
인스타 피드 컬러 슬라이드에도 반영 (2026-08-18 신고)
  A 에서 상세만 고쳤다. 피드(nvfColor)는 자기 설정(NVF.color)을 쓰고 있어서
  흰 박스도 새 치수도 안 붙었다.
  .fig 에 처마 피드가 없으므로 상세 실측값을 캔버스 폭 비율로 키웠다
  (1080/860 = 1.2558) — 향 섹션과 같은 방식. 두 산출물의 비례가 같아진다.
    NVF_COLOR_SHARED  박스 x63 w954 여백 65 · 칩 438 · 좌우 15 · 행 25 · 이름 35/50

  ⚠ 그대로 키우면 캔버스를 넘는다
    5색 3행 → 박스 끝 y 1491 · 캔버스 1350 → 141px 초과.
    남는 세로에 맞춰 사진 높이를 줄인다(이름칸·행간격은 유지).
      329×183 · 박스 끝 y 1310 · 맨 아래 칩 1245  → 1350 안에 들어온다.
    상세는 섹션 높이가 늘어나므로 이 축소가 없다.

  누볼라 피드는 예전 그대로(흰 박스 없음 · 칩 7개).

이전:
처마 컬러 섹션 — cheoma_color.fig 실측 반영 + 흰 박스 (2026-08-18)
  공통(shared) 모드 치수를 .fig 값으로 확정했다. 누볼라(lines)는 예전 그대로.

                지금                    .fig 실측
    흰 박스     없음(01·02)             x50 w760 · 위아래 여백 52 · #ffffff
    칩 폭       비율에서 계산            349 고정
    칩 좌우     12                      12  (같음)
    행 간격     38                      20
    이름 글자   24px                    28px
    사진~이름   4                       0
    배치        2열 1-2-2               2열 1-2-2  (같음)

  흰 박스는 템플릿 01·02·03 모두 깐다 — 제품 사진이 배경에 묻히던 문제.
  누볼라 03 의 기존 흰 박스(폭 800)는 그대로 뒀다.
  칩 높이는 349 / 사진비율 로 나온다(9:5 사진 → 194).
  colorRowGap 을 시트에 적으면 여전히 덮어쓸 수 있다(기본 20).

  ⚠ .fig 은 첫 칩만 사진 높이가 156, 나머지는 100 이다(사진마다 여백이 달라서로 보인다).
    코드는 한 제품군에 한 비율을 쓰므로 전부 같은 높이로 그린다.
    사진마다 높이를 다르게 하려면 별도 작업이 필요하다.
