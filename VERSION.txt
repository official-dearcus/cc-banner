cc-banner  build 2026-08-11-C
02 옵션 이미지 아래 여백 0 → 20 (요청 2026-08-11)
  -B 에서 02 만 imgPadBot 0 이라 이미지가 구분선에 딱 붙었다.
  20 으로 두면 컨테이너 패딩(20)·카드 간격(20)과 같아져서
  구분선이 [이미지 끝 20] 선 [20 이미지 시작] 한가운데에 온다.

왼쪽 편집 패널에서 옵션 줄을 직접 수정 (전 제품군 공통, 11-rows-picker)
  카드마다 [옵션 수정] 버튼. 누른 카드만 입력창으로 바뀐다.
    · 줄마다 [라벨][값][×]  ·  [+ 줄 추가]
    · 편집 상태는 r._edit 로 행이 들고 있다 → 순서 변경·재렌더에도 열린 채 유지
    · 입력 중에는 renderRows() 를 안 부른다 (커서 튐 방지). draw() 로 캔버스만 갱신
    · 구형 unit 문자열 행도 normAttrs() 가 배열로 바꿔 편집 가능
    · colorLine 이 걸린 행은 "Color 줄은 컬러 선택에서 자동" 안내를 띄운다
      (09-hero-color cardAttrs 가 덮어쓰므로 여기서 고쳐도 배너엔 안 나온다)
  _edit 는 화면 상태라 저장·공유 payload(20-history-share)엔 안 들어간다.
  styles/04-panels.css 에 .roedit / .rorow / .ronote 추가.

이전:
cc-banner  build 2026-08-11-B
누볼라 상세 옵션 카드 — 이미지 높이가 카드 높이를 따라가게
