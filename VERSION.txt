cc-banner  build 2026-08-11-A
누볼라 상세 옵션 카드 value_list 를 .fig 2026-08 최신값으로 동기화
  값·라벨 텍스트  18px → 22px  (라벨 Bold #666 / 값 Regular #888)
  행 높이(vlRowH) 24 → 30 (01·02) / 28 (03)
  라벨:값 비율(fr)
    01  1.2:4  라벨56 · 값186  (값 x=64)
    02  1:4    라벨67 · 값267  (값 x=75)
    03  1:4    라벨60 · 값274  (값 x=68, 그대로)
  옵션 카드 높이  고정 360 → 내용 기반 가변(hug)
    cardH = textDY + name-wrap + cardGap + priceH + cardPadBot
    01  30 + … + 24 + 82 + 30 = 약 450   (.fig option_01 740×450)
    02  28 + … + 36 + 80 + 52 = 약 358   (.fig 720×358)
    03   0 + … + 36 +100 +  0 = 약 368   (.fig 720×368)
    → 예전 고정 360 은 값이 2줄이면 가격줄과 겹쳤다(첫 카드 RENEWAL). 이제 안 겹친다.
  nvMeasureCard() 신설 — nvListItems(높이 배분)와 nvCard(그리기)가 같은 높이를 쓴다.

뱀부 상세 단위가 텍스트도 18px → 22px 로 통일 (02-design-tokens unit.size).
  뱀부는 .fig 에 Color/Size value_list 가 없어 레이아웃은 유지, 글자 크기만 맞춤.
  피드/썸네일 값 텍스트는 이번 범위 아님 (그대로).

줄바꿈 단어단위 개선 (16-render-cards wrapText).
  공백뿐 아니라 쉼표(, 、) 뒤도 끊는 지점. 공백 없는 "화이트,핑크,딥그린" 도
  색 이름이 통째로 유지되고 쉼표 뒤에서 접힌다. 원문 글자는 그대로 보존.
  끊을 지점이 전혀 없는 긴 단어만 글자 단위 최후 분할.

이전:
cc-banner  build 2026-07-29-A
인스타 피드 옵션 카드 — 값 열(Color/Size)을 템플릿별로 분리 (.fig 2026-07-29 feed_02)
