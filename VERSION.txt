cc-banner  build 2026-09-01-F
1) 뱀부에서 써볼래요 이벤트가 안 뜨던 문제 (2026-09-01 신고)
   legacySecOrder 의 키 목록에 "try" 를 빠뜨렸다 → 피드 계획에서 통째로 잘렸다.
   상세에는 안 그려지지만(LEGACY_SECTIONS 에 없다) 피드 순서에는 있어야 한다.
   hero → opt  ⇒  hero → opt → try  (feedCount 2 → 3)

2) 한 장에 이벤트 3개까지 · 아래 여백 축소 (요청 2026-09-01)
   EVF.bottom 40 → 20 · maxPerSlide 3 신설.
   ⚠ 아래 여백만으로는 부족했다 — .fig 카드 크기 기준 3장 필요/가용:
       01  1141 / 975   02  1003 / 984   03  1031 / 963
     여백을 0 으로 해도 01·03 은 안 들어간다.
   → 3개가 빠듯하면 "카드만" 조금 줄인다(evfShrinkFor). 제목·상단 여백은
     그대로라 앞서 맞춘 상단 정렬이 유지된다. 최소한만 줄인다(1.00→0.84).
       실제 배율  01 0.85 · 02 0.99 · 03 0.87
       맨 아래 y  1326 / 1327 / 1319  (캔버스 1350)
     2개 이하일 땐 배율 1 — 지금까지와 크기가 같다.
     4개면 3+1 로 나뉜다. 써볼래요도 같은 규칙을 쓴다.
   ⚠ 01 이 제일 많이 줄어든다(15%). 본문 폭이 740 이라 피드 확대율이
     1.297 로 제일 크기 때문이다. 눈으로 보고 과하면 알려줄 것.

이전:
이벤트·써볼래요 피드 상단을 다른 슬라이드와 맞춤 (요청 2026-09-01)
  before  세로 가운데 정렬이라 카드 장수에 따라 제목이 오르내렸다.
          1장짜리 써볼래요는 한참 아래에서 시작했다.
  after   EVF.topY 115 고정 — 컬러(txtY 115) · 옵션(headY 118)과 같은 자리.
          장수와 무관하게 늘 같은 높이에서 시작한다.

  한 장에 담는 개수를 "재서" 정한다 (evFeedFit)
    카드 높이가 문장 길이에 따라 다르므로(2줄/3줄) 고정 2장으로 나누면
    어떤 조합은 넘치고 어떤 조합은 자리가 남았다.
    가용 높이 = 1350 - topY 115 - 아래여백 40 - 헤더 - headGap
    들어가는 만큼 채우고 넘치면 다음 장으로. 써볼래요도 같은 함수를 쓴다.

  ⚠ "3개까지 들어갈 것 같다" — .fig 카드 크기로는 안 들어간다
      템플릿  가용   3장 필요   결과
        01     963     1106    143px 초과
        02     984     1003     19px 초과
        03     943     1031     88px 초과
    아래여백을 20 으로 줄여도 02 만 1px 여유로 겨우 들어간다(실질적으로 무리).
    지금은 2장이 안전한 최대다. 3장을 꼭 넣으려면 카드 높이나 헤더 여백을
    줄여야 하고, 그건 .fig 과 어긋나므로 확인 후 진행할 일이다.
